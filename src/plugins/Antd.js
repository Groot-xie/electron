import Vue from 'vue'
import {
  Button,
  Table,
  message,
  Input,
  Checkbox,
  Tag,
  Icon,
  Select,
} from 'ant-design-vue'

Vue.prototype.$message = message

Vue.use(Button)
Vue.use(Table)
Vue.use(Input)
Vue.use(Checkbox)
Vue.use(Tag)
Vue.use(Icon)
Vue.use(Select)